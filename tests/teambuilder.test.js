import TeamBuilder from "../lib/teambuilder.js";

describe("TeamBuilder", () => {
    it("Returns a TeamBuilder object with team, teamMembers, teamRoles, managerRoles, and prompts properties", () => {

    });

    it("Returns a TeamBuilder object with appropriate team, teamMembers, teamRoles, managerRoles, and prompts values when instantiated.", () => {

    });

    describe("init()", () => {

    });

    describe("build(promptKey)", () => {

    });

    describe("addTeamMember(key, value)", ()=> {

    });

    describe("getTeamMember(key)", ()=> {

    });

    describe("addTeamRole(role)", ()=> {

    });

    describe("addManagerRole(role)", ()=> {

    });

    describe("getTeam()", ()=> {

    });
});